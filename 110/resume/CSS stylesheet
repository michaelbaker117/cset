/*changed background color, font, font, size, centered text*/
body {
  background: radial-gradient(rgba(255, 238, 0, 0.233),white,aqua);
  font-family: cursive, monospace;
  font-size: 16pt;
  text-align: center;
}

/*added border around image*/
img {
  width: 500px;
  border-color: black;
  border-width: 5px;
  border-style: groove;
}

/*added an underline under header 1 and 2*/
h1, h2 {
  text-decoration: underline black;
}

/*changed text color*/
p {
  color: darkblue;
}

/*changed text color*/
p strong {
  color: black;
}

/*changed link color*/
a {
  color: blueviolet;
}

/*added bakground color when mouse is hovering over link*/
a:hover {
  background-color: aliceblue;
}

/*changed font size and text align left*/
footer {
  font-size: 10pt;
  text-align: left;
}
